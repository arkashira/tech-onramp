# Curriculum Engine & Content Delivery

This project provides a curriculum engine that loads modules from Markdown files, parses metadata, and publishes modules.

## Usage
1. Create a `content/modules` directory with Markdown files containing module metadata.
2. Run `python -m pytest` to test the curriculum engine.
3. Use the `CurriculumEngine` class to load and publish modules.
